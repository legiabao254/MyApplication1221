<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".authapp.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".authapp.RegisterActivity"
            android:exported="false"
            android:label="Đăng ký" />

        <activity
            android:name=".authapp.AdminActivity"
            android:exported="false"
            android:label="Quản lý Slot" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".ProfileDetailActivity"
            android:exported="false"
            android:label="Thông tin Cá nhân"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <activity
            android:name=".ParkingSlotActivity"
            android:exported="false"
            android:label="Danh sách Bãi đỗ" />

        <activity
            android:name=".ParkingActivity"
            android:exported="false"
            android:label="Gửi/Lấy Xe" />

        <activity
            android:name=".PaymentActivity"
            android:exported="false"
            android:label="Thanh toán" />

        <activity
            android:name=".LPRScannerActivity"
            android:exported="false"
            android:label="Quét Biển số"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".ParkingHistoryActivity"
            android:exported="false"
            android:label="Lịch sử Gửi xe" />

        <!-- === KHAI BÁO CHO CÁC TÍNH NĂNG MỚI === -->

        <activity
            android:name=".TopUpActivity"
            android:label="Nạp tiền vào Ví"
            android:exported="false" />

        <activity
            android:name=".AdminConfirmDepositActivity"
            android:label="Xác nhận Nạp tiền"
            android:exported="false" />

        <!-- SỬA LỖI: Chỉ giữ lại một khai báo duy nhất cho AdminReportActivity -->
        <activity
            android:name=".AdminReportActivity"
            android:label="Báo cáo Doanh thu"
            android:exported="false" />

    </application>
</manifest>
